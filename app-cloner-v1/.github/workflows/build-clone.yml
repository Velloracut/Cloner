name: Build App Clone

on:
  workflow_dispatch:
    inputs:
      app_name:
        description: Clone app name
        required: true
        type: string
      application_id:
        description: Android applicationId
        required: true
        type: string
      version_name:
        description: Version name
        required: true
        default: 1.0.0
        type: string
      version_code:
        description: Version code
        required: true
        default: "1"
        type: string
      output:
        description: Output format
        required: true
        default: apk
        type: choice
        options: [apk, aab]
      build_type:
        description: Build type
        required: true
        default: release
        type: choice
        options: [debug, release]

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Setup JDK 17
        uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: "17"

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v5
        with:
          cache-read-only: false
          gradle-home-cache-cleanup: true

      - name: Validate
        shell: bash
        run: |
          set -euo pipefail
          APP_ID="${{ inputs.application_id }}"
          VERSION_CODE="${{ inputs.version_code }}"
          if [[ ! "$APP_ID" =~ ^[A-Za-z_][A-Za-z0-9_]*(\.[A-Za-z_][A-Za-z0-9_]*)+$ ]]; then
            echo "Invalid applicationId"; exit 1
          fi
          if ! [[ "$VERSION_CODE" =~ ^[1-9][0-9]*$ ]]; then
            echo "Invalid versionCode"; exit 1
          fi

      - name: Prepare clone
        shell: bash
        env:
          APP_NAME: ${{ inputs.app_name }}
          APPLICATION_ID: ${{ inputs.application_id }}
          VERSION_NAME: ${{ inputs.version_name }}
          VERSION_CODE: ${{ inputs.version_code }}
        run: |
          set -euo pipefail
          rm -rf clone-workspace
          cp -R app clone-workspace
          python3 - <<'PY'
          from pathlib import Path
          import os
          root = Path("clone-workspace")
          replacements = {
              "__APP_NAME__": os.environ["APP_NAME"],
              "__APPLICATION_ID__": os.environ["APPLICATION_ID"],
              "__VERSION_NAME__": os.environ["VERSION_NAME"],
              "__VERSION_CODE__": os.environ["VERSION_CODE"],
          }
          for p in root.rglob("*"):
              if not p.is_file() or p.suffix not in {".kt",".kts",".xml",".gradle",".properties",".json"}:
                  continue
              try:
                  s = p.read_text(encoding="utf-8")
              except UnicodeDecodeError:
                  continue
              for a,b in replacements.items():
                  s = s.replace(a,b)
              p.write_text(s, encoding="utf-8")
          PY

      - name: Prepare Gradle project
        run: |
          cp settings.gradle.kts clone-workspace/
          cp build.gradle.kts clone-workspace/
          cp gradle.properties clone-workspace/
          if [ -f gradlew ]; then cp gradlew clone-workspace/; chmod +x clone-workspace/gradlew; fi

      - name: Build debug APK
        if: inputs.build_type == 'debug'
        working-directory: clone-workspace
        run: ./gradlew assembleDebug

      - name: Build release APK
        if: inputs.build_type == 'release' && inputs.output == 'apk'
        working-directory: clone-workspace
        run: ./gradlew assembleRelease

      - name: Build release AAB
        if: inputs.build_type == 'release' && inputs.output == 'aab'
        working-directory: clone-workspace
        run: ./gradlew bundleRelease

      - name: Upload APK
        if: inputs.output == 'apk'
        uses: actions/upload-artifact@v4
        with:
          name: ${{ inputs.app_name }}-apk
          path: clone-workspace/app/build/outputs/apk/**/*.apk
          if-no-files-found: error

      - name: Upload AAB
        if: inputs.output == 'aab'
        uses: actions/upload-artifact@v4
        with:
          name: ${{ inputs.app_name }}-aab
          path: clone-workspace/app/build/outputs/bundle/**/*.aab
          if-no-files-found: error
