<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!--
      Required on Android 11+ (API 30+) to see the full list of installed
      apps via PackageManager. Without this, queryIntentActivities() only
      returns a small subset of "visible" packages.

      NOTE: Google Play restricts this permission — apps that request it
      must declare an approved use-case in Play Console (e.g. "app
      management" / "device search"), or the listing can be rejected.
      See: https://support.google.com/googleplay/android-developer/answer/10158779
    -->
    <uses-permission android:name="android.permission.QUERY_ALL_PACKAGES"
        tools:ignore="QueryAllPackagesPermission"
        xmlns:tools="http://schemas.android.com/tools" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="Cloner"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.DualApp">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="Cloner"
            android:theme="@style/Theme.DualApp">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>
