<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Needed on Android 11+ to see the full installed-apps list -->
    <uses-permission android:name="android.permission.QUERY_ALL_PACKAGES"
        tools:ignore="QueryAllPackagesPermission" />

    <!-- Needed by the virtual engine to read/copy target APKs off disk -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />

    <!--
      CRITICAL for cloned apps: they run inside OUR process/UID, so THEIR
      network access is governed by OUR permissions, not whatever they
      declare in their own manifest. Missing this caused every network
      call from every cloned app to fail (confirmed root cause of
      InvestAndEarn's "net::ERR_CACHE_MISS" — WebView had no usable network
      access at all, not just a cache problem).
    -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />

    <!--
      Same reasoning as INTERNET above: a cloned app can only ever be
      GRANTED these at runtime (the user still sees Android's normal
      permission popup, attributed to Cloner) if they're declared here —
      otherwise the request silently can't even be asked for. Covers the
      common cases (camera/mic for video/photo editors, media library
      access, notifications, location) so we don't have to rediscover each
      one via another crash-and-rebuild cycle.
    -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" />
    <uses-permission android:name="android.permission.READ_MEDIA_AUDIO" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.VIBRATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="Cloner"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.DualApp">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="Cloner"
            android:theme="@style/Theme.DualApp">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--
          Proxy activities: the virtual engine launches a cloned app's real
          Activity *as if* it were one of these stub activities, so the
          system only ever sees activities that are actually declared here.
          Phase 2 will register several of these (different launch modes /
          task affinities) as the hooking layer is built out.
        -->
        <activity
            android:name=".virtual.VirtualStubActivity"
            android:exported="false"
            android:theme="@style/Theme.DualApp" />

    </application>

</manifest>
